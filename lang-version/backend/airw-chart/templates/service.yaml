apiVersion: v1
kind: Service

metadata:
  name: airw-backend-service
  namespace: airw

spec:
  type: {{ .Values.service.type }}

  selector:
    app: airw-backend

  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}