apiVersion: batch/v1
kind: Job

metadata:
  name: airw-test-job
  namespace: airw

spec:
  backoffLimit: 3

  template:
    spec:
      restartPolicy: Never

      containers:
        - name: test-job
          image: busybox:1.36
          command:
            - sh
            - -c
            - echo "Hello from Kubernetes Job!"